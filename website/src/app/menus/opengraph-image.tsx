/* Re-exported so the file convention resolves for this route segment too;
   without it a page that defines its own `openGraph` loses the card image. */
export { default, alt, size, contentType } from "../opengraph-image";
